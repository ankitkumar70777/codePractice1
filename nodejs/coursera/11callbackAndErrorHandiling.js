





/*callbacks and error handiling : 
1) first class function : a function which can be treated as 
same way as any other veriabele,
2) closures :  
    -a function defined inside another function has 
 has access to all veriable and their scopes in the outer 
 function(outer scope)  
    - the inner function wil continue to have access 
    to the veriabele from the oyter scope even after the outer 
    function has returned
3) learn functional programming : 
4)  
*/