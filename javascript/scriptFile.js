//In programming, inheritance refers to passing down characteristics from a parent
// to a child so that a new piece of code can reuse and build upon the features of an existing one.
// JavaScript implements inheritance by using objects. Each object has an internal link to another object called its prototype.
// That prototype object has a prototype of its own, and so on until an object is reached with null as its prototype.
// By definition, null has no prototype and acts as the final link in this prototype chain.

//Inheritance with the prototype chain

// Prototpyes in JavaScript are nothing but a special set of properties held by an object.
